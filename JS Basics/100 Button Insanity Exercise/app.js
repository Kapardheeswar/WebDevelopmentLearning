// WRITE YOUR CODE IN HERE:
for (let i = 0; i < 100; i++) {
    const button=document.createElement("button");
    button.innerText="KK Button";
    container.insertAdjacentElement("afterbegin", button);
    // container.appendChild(button);
}